#include "stdio.h"
int main(int argc, char const *argv[]) {

int myInt;
int  *pPointer;
struct MyStruct dvorak;
struct MyStruct *pKeyboard;
pPointer = &myInt;
pKeyboard=&dvorak;

struct MyStruct *pkeyboard;

pKeyboard = malloc(sizeof  *pKeyboard)  //The malloc function returns a pointer to dynamically allocated memory



  return 0;

}
